import {
	containerQueries,
	mediaQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
} from "@/defaults/variants";

export const standard = {
	pseudoClasses,
	pseudoElements,
	mediaQueries,
	containerQueries,
};

export const withOpacity = {
	mediaQueries,
	containerQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
};
