import {
	mediaQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
} from "@/defaults/variants";

export const standard = {
	pseudoClasses,
	pseudoElements,
	mediaQueries,
};

export const withOpacity = {
	mediaQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
};
