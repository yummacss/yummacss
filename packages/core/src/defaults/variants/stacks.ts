import {
	containerQueries,
	mediaQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
} from "@/defaults/variants";

export const base = {
	pseudoClasses,
	pseudoElements,
	mediaQueries,
	containerQueries,
};

export const all = {
	mediaQueries,
	containerQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
};
