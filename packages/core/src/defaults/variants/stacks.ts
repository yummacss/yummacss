import {
	mediaQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
} from "@/defaults/variants";

export const base = {
	pseudoClasses,
	pseudoElements,
	mediaQueries,
};

export const all = {
	mediaQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
};
