import {
	mediaQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
} from "@/defaults/variants";

export const standardPreset = {
	pseudoClasses,
	pseudoElements,
	mediaQueries,
};

export const opacityPreset = {
	mediaQueries,
	opacity,
	pseudoClasses,
	pseudoElements,
};
