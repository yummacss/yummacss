## Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Features

- No user-facing changes.

## [1.7.2] - 2025-10-11

### Breaking changes

- Changed `toHexString()` function instead of `toHex8String()`.

### Fixes

- Fixed color type definitions to generate valid CSS.

## [1.7.1] - 2025-10-11

### Breaking changes

- Changed `toHex8String()` function instead of `toHexString()`.

### Fixes

- Fixed color type definitions to generate valid CSS.

## [1.7.0] - 2025-10-10

### Features

- Added `opacity` variant to all color utilities.

### Fixes

- Fixed missing `type` keyword in interface imports.

## [1.6.2] - 2025-10-02

### Fixes

- Fixed `tdt-*` (text-decoration-thickness) utilities to use `px` instead of `rem`.

## [1.6.1] - 2025-10-02

### Fixes

- Fixed `font-family` utilities delimiters.

## [1.6.0] - 2025-10-02

### Features

- Added `variant` interface.
- Added media query variants (`sm:`, `md:`, `lg:`, `xl:`, `xxl:`) support to all utilities.
- Added pseudo-class variants (`a:`, `f:`, `h:`) support to all utilities.
- Updated `Utility` interface to include `variants` property.

## [1.5.0] - 2025-09-23

### Breaking changes

- Renamed `getAllUtils` to `coreUtils`.
- Renamed `getBackgroundUtils` to `backgroundUtils`.
- Renamed `getBorderUtils` to `borderUtils`.
- Renamed `getBoxModelUtils` to `boxModelUtils`.
- Renamed `getColorUtils` to `colorUtils`.
- Renamed `getEffectUtils` to `effectUtils`.
- Renamed `getFlexboxUtils` to `flexboxUtils`.
- Renamed `getFontUtils` to `fontUtils`.
- Renamed `getGridUtils` to `gridUtils`.
- Renamed `getInteractivityUtils` to `interactivityUtils`.
- Renamed `getOutlineUtils` to `outlineUtils`.
- Renamed `getPositioningUtils` to `positioningUtils`.
- Renamed `getSvgUtils` to `svgUtils`.
- Renamed `getTableUtils` to `tableUtils`.
- Renamed `getTextUtils` to `textUtils`.
- Renamed `getTransformUtils` to `transformUtils`.

## [1.4.1] - 2025-09-18

### Fixes

- No user-facing changes.

## [1.4.0] - 2025-09-15

### Breaking changes

- Renamed `ColorUtilityItem` interface to `Color`.
- Renamed `ColorUtilityMap` interface to `Colors`.
- Renamed `UtilityItem` interface to `Utility`.
- Renamed `UtilityMap` interface to `Utilities`.
- Removed `ApiResponse` type.

## [1.3.1] - 2025-09-11

### Fixes

- No user-facing changes.

## [1.3.0] - 2025-08-23

### Features

- ?

## [1.2.0] - 2025-08-23

### Features

- ?

## [1.1.2] - 2025-07-31

### Features

- ?

## [1.1.1] - 2025-07-29

### Features

- ?

## [1.1.0] - 2025-07-29

### Features

- ?

## [1.0.0] - 2025-07-11

### Features

- Initial release.
