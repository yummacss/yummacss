export const config = {
  content: [], // Specify paths for CSS scanning.
  output: "", // Path for saving CSS file.
  capabilities: {
    reset: true, // Should I use base styles? — Optional (Default is true)
    minify: true, // Should I minify? — Optional (Default is true)
  },
};
