import { mediaQueries, opacity, pseudoClasses } from "@/defaults/variants";

export const variantValues = {
  pseudoClasses,
  mediaQueries,
};

export const colorVariantValues = {
  pseudoClasses,
  mediaQueries,
  opacity,
};
